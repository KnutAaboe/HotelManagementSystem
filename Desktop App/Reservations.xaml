<Window x:Class="Desktop_App.Reservations"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktop_App"
        mc:Ignorable="d"
        Title="Reservations" Height="450" Width="800">
    <Grid>
        <ListView Name="reservationList" ItemsSource="{Binding}" Margin="0,38,0,0">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Width="100" Header="roomNr" DisplayMemberBinding="{Binding Path=roomNr}" />
                        <GridViewColumn Width="100" Header="username" DisplayMemberBinding="{Binding Path=username}" />
                        <GridViewColumn Width="100" Header="startDate" DisplayMemberBinding="{Binding Path=startDate}" />
                        <GridViewColumn Width="100" Header="endDate" DisplayMemberBinding="{Binding Path=endDate}" />
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
